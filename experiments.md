# Протокол экспериментов

Живой журнал проекта: каждый прогон с его конфигурацией, результатом и выводом.
Заполнялся сразу после каждого эксперимента, ни один вывод не опирается на память.
Здесь полная история; в README — выжимка.

---

## Методология

- **Одна переменная за прогон.** Сравнивать можно только прогоны, отличающиеся одной вещью; всё остальное (сплит, сид, лосс, эпохи) зафиксировано.
- **Фиксированный val:** сэмпл 1000 из 80k (seed 42), стратифицированный сплит 90/10 + все доступные чистые (orgl) в val → **147 картинок (50 чистых).**
- **Метрика решения:** grid-searched score (порог × min_area × reject × gate) на prob-картах в нативном разрешении — не in-loop прокси на 576².
- **Порог значимости:** |Δ| < 0.02 на val из 147 картинок = шум, не «буст».
- **Чекпоинт** — по лучшему per-image скору (не батчевому среднему).
- **Эквивалент-тесты рефакторинга:** бит-в-бит снапшоты данных до/после любого изменения датасетов (32 сэмпла, `max_diff == 0.0`).

---

## Этап 0: архитектурный бенчмарк

6 эпох, полный датасет партнёра, выбор декодер/энкодер-пары до основного цикла.

| Модель | params | GFLOPs | Dice | Clean | Score | Судьба |
|---|---|---|---|---|---|---|
| Unet + resnet34 | 24.4M | 80.4 | 0.500 | 0.922 | 0.711 | база |
| Unet + eff-b3 | 13.2M | 39.2 | 0.646 | 0.954 | 0.800 | **выбрана** |
| FPN + resnet34 | 23.2M | 70.7 | 0.506 | 0.934 | 0.720 | отклонена |
| MAnet + resnet34 | 31.8M | 85.6 | — | — | NaN | коллапс (NaN loss) |
| DeepLabV3+ + eff-b3 | 11.7M | 30.6 | 0.649 | 0.938 | 0.794 | отклонена: лучший Dice, но Clean ниже |

> **Вывод:** выбор определила бизнес-метрика, а не Dice. DeepLabV3+ выигрывал сегментацию — вылетел по Clean (precision-leaning).

---

## Основной цикл (AB-конвейер)

Вал: фиксированные 147 картинок. Якорные цифры — grid-searched score.

| # | Эксперимент | Score | Что менялось | Вывод | Следствие |
|---|---|---|---|---|---|
| 1 | Sanity (smp.utils цикл) | 0.6754 | стартовая конфигурация | механика работает | найден BOM-баг csv (терялись все orgl → 0 чистых в train), чистые в val: 2 → 50 |
| 2 | Свой цикл + спека-лосс | 0.7215 | CombinedLoss 0.5/0.5/1.0, aux в контуре, bf16-AMP, per-image метрики | aux: 0.48 → 0.80 accuracy | новый канонический цикл (`src/loop.py`) |
| 3 | Forensics-каналы | 0.7131 | ELA + High-pass (5-канальный вход) | Dice нейтрален, aux 0.86 (FN 13→7) | каналы — image-level сигнал; оставлены (усиливают aux) |
| 4 | Трансформер mit_b1 | 0.6052 | энкодер A/B, ёмкость сопоставима | dice 0.0 на 1–3 эпохах, линейный рост после | холодный старт на малых данных; ветка закрыта |
| 5 | Aux-гейт | +0.0006 | зануление маски при aux < порога | FP на чистых живут при aux > 0.5 | гейт не лечится постпроцессингом; оставлен в grid search |
| 6 | Аугментации (флипы) | 0.7197 | H/V-флипы, геометрия до ELA | train-val разрыв сжался (0.48/0.82) | в основной пайплайн; фотометрия запрещена (ломает ELA) |
| 7 | Ансамбль ×3 | 0.7256 | усреднение prob-карт трёх моделей | +0.004 к лучшей одиночной | работает при сопоставимости компонентов |
| 8 | Кропы + слайдинг | 0.7374 | нативные кропы 576² (train) + окна 1/3 (inference) | +0.018 к лучшему; dice 0.495 in-loop (лучший результат) | разрешение — главный рычаг; патч в `train.py` (`--use-crops`) |

---

## Диагностические находки

> Не были экспериментами, но определили решения.

- **BOM в csv:** `utf-8` вместо `utf-8-sig` → ключ `orgl_img_path` читался с кавычками → все оригиналы терялись → 0 чистых в train. Найден на день 1 проверочным сниппетом, а не по «странной» метрике.

- **24/1000 чистых:** естественная доля 2.4% → без балансировки Clean измерялся бы по 2 картинкам. Балансировка orgl до 30% в train, все чистые в val.

- **Локализация дефицита Clean:** 14 из 50 val-чистых дают весь дефицит Clean (FP ~12% на каждой), у всех aux > 0.5 — aux-FP и seg-FP коррелируют. Лечение — данные/обучение, не постпроцессинг.

- **Лосс под autocast:** суммы по 256² проходят впритык (65536 vs fp16-max 65504), по 576² — гарантированный `inf`. Лосс всегда в fp32.

- **Вал на 147 картинках:** два прогона одной конфигурации различаются на ~0.01 — источник фиксированного порога значимости 0.02.

---

## Финал (внешняя оценка)

| Конфигурация | Score |
|---|---|
| 80k, соло: кропы + слайдинг + гейт + TTA | 0.89092 |
| Ансамбль 80k + 20k | 0.89099 |
| Ресайз-инференс (без окон) | 0.8888 |
| Голый бейзлайн (без инференс-стека) | ~0.89 |

### Разбор финала

- Стек инференса дал **+0.001–0.002** к бейзлайну — основной вклад в результат внесли данные (80k) и обучение (кропы), а не инференс-слой.
- Ансамбль 80k+20k = **+0.0001** — модели одного пайплайна с пересекающимися данными почти идентичны; подтверждение урока #7 (разнообразие ≠ количество).
- Разрыв val (0.86+) → внешняя оценка (0.891): val был контаминирован (~90% его строк входило в трейн 80k-рана) и принадлежал тому же синтетическому домену. Реальная генерализация оказалась ниже val-ожиданий, но выше наших страхований — доменный сдвиг теста оказался умеренным.

---

## Что сделали бы иначе

- **Первый сабмит — в первый день.** Вся калибровка порогов шла по контаминированному прокси; внешняя оценка пришла в последний день.
- **Чистый held-out для тюнинга порогов** — обязательный артефакт, а не «когда-нибудь потом».
- **Каждая техника — инкремент на внешней оценке, не на val:** слайдинг-виндоу дал +0.027 на val и +0.002 на финале.

## Отложенные ветки (рассмотрено, не проведено)
- Pseudo-labeling (nanobanana). Исходный план предполагал генерацию дополнительных правок предоставленным
  инструментом и слабые псевдо маски из diff-пар с весом ~0.3 (заготовка — поле is_pseudo_label в data.py, веса sample_weight в лоссе).
  Отложено по трём причинам:
  1) Приоритет. Ожидаемый прирост оценивался ниже, чем у разрешения/кропов (эксперимент #8) — время GPU ушло туда, где прогноз был сильнее
  2) Технические риски: ген-модель перерендеривает картинку целиком forensics сигнатура такой правки не похожа на локальный inpainting (риск доменного сдвига внутри трейна); diff-маски зашумлены двойным JPEG и требуют         отдельной чистки.
  3) Ретроспектива: финальный анализ показал, что узкое место — доменныйсдвиг train→test. Добавление ещё синтетики того же семейства скорееусилило бы сдвиг, чем закрыло его.

- Кратко о прочих отложенных ветках: SRM-канал (та же high-pass-семья, слабый кандидат под inpainting правки), EMA/seed-ансамбль (финальный рывок — недошли по времени), c lDice-лосс (зависел от подтверждения проблемы тонких масок кропами).

---
